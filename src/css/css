.root {
    -fx-background-color: #F4F6F9;
    -fx-font-family: "Segoe UI";
}

.button {
    -fx-background-radius: 8;
    -fx-font-weight: bold;
}

.text-field,
.combo-box,
.text-area {
    -fx-background-radius: 8;
}

.table-view {
    -fx-background-radius: 8;
}